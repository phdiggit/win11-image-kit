# Packages

Do not commit large installers, archives, commercial packages, licenses, or private credentials.

Use NAS paths instead:

```text
\\192.168.1.37\backups\win11-image-kit\packages
```

Record package names, versions, source URLs, and SHA256 checksums in manifests or checksum files.
