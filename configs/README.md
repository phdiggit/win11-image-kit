# Configs

Put versionable templates here:

- Windows Terminal settings template
- VSCode portable defaults
- Default app associations XML
- Start menu layout backup notes
- JetBrains exported settings notes

Do not commit secrets, account tokens, or license files.
